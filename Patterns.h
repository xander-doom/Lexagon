#ifndef PATTERNS_H
#define PATTERNS_H

#include <cstdint>
#include <vector>
#include "FastLEDMath.h"

using namespace std;

vector<uint32_t> pride(vector<uint32_t>& pixels, int numLeds, int deltams);

#endif