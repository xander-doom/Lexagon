#ifndef PATTERNS_H
#define PATTERNS_H

#include <cstdint>
#include <vector>
#include "FastLED.h"

using namespace std;

void pride(CRGB leds[], int numLeds);
void pacifica(CRGB leds[], int numLeds);

#endif